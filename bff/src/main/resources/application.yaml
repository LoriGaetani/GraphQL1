spring:
  application:
    name: bff
  # just for development purposes
  graphql:
    graphiql:
      enabled: true
server:
  port: 8081

services:
  book-service:
    base-url: http://localhost:8080
